Place the extracted Oracle Instant Client 19+ Linux x64 files in this folder before building the Hostinger image.

Expected runtime path inside the container:
- /opt/oracle/instantclient

Expected files include:
- libclntsh.so
- libocci.so
- libnnz*.so
